name: starmesh
description: Starmesh v2 — Starlink hub-and-spoke messenger.
publish_to: "none"
version: 2.0.0
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  starmesh_invite:
    path: ../invite/dart
  qr_flutter: ^4.1.0
  http: ^1.2.1
dev_dependencies:
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
