# Copy to ~/.config/starmesh/starmesh.env or /etc/starmesh/starmesh.env
STARMESH_NAME=tasai-lab
STARMESH_API=0.0.0.0:7780
# STARMESH_API_TOKEN=change-me
# Without global IPv6 the unit must pass --dev (see the service file).
